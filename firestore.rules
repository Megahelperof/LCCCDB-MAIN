service cloud.firestore {
  match /databases/{database}/documents {
    match /Admin/AdminUser/{userId} {
      allow read, write: if request.auth != null; // Allow access only if user is authenticated
    }
    match /Admin/AdminUser/{userId}/info {
      allow read: if true;  // Allow read access to 'info' documents for testing
    }
  }
}
